h1. Jquery Codec

Jquery codec can be used to convert a Closure to a Javascript object, which can be used anywhere to render
 to a String. No imports required in this case.

_Example_
{code}
{ jq ->
    jq("#main").show()
}.encodeAsJquery().toString()
{code}
_Output_
{code}
$("#main").show();
{code}